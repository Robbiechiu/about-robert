# 維護指南 — 羅伯特農業科技 數位名片頁

> 這份是給「快速接手修改」用的。改完一律 `git add -A && git commit -m "..." && git push origin main`，
> GitHub Pages 約 30–60 秒自動更新。**驗證版面請用真手機或 ≥500px 寬**（見最後「注意事項」）。

線上：<https://robbiechiu.github.io/about-robert/>　·　Repo：`Robbiechiu/about-robert`

---

## 1. 檔案結構

```
robert-intro-page/
├── index.html          ← 全部在這一個檔（HTML + CSS + JS 內嵌）
├── assets/             ← 圖片
│   ├── greenhouse-hero.jpeg   首屏溫室照
│   ├── robert-jump.jpeg       西裝跳躍照（一句話定位段）
│   ├── robert-lettuce.jpeg    形象照（關於我段）
│   ├── qr-line.png / qr-ig-*.png  QR圖（目前已改按鈕、未使用，先留著）
│   └── _original/      未裁切的原始 QR（.gitignore 排除，不上線）
├── CHANGELOG.md
└── 維護指南.md（本檔）
```

`index.html` 內以註解分段，搜尋這些字串可快速跳到：
`<!-- ===== HERO ===== -->`、`STATEMENT`、`STATS`、`VENTURES`、`ABOUT`、`CONTACT`、`<footer>`。

---

## 2. 設計規格（CSS 變數，在 `:root{}` 最上方）

| 變數 | 值 | 用途 |
|---|---|---|
| `--bg` / `--bg-2` / `--card` | 深綠系 | 背景／卡片 |
| `--green` / `--green-bright` / `--green-deep` | 綠 | 標籤、點綴 |
| `--gold` | `#cba24e` | **主要金**（品牌字、重點） |
| `--gold-light` | `#e7cd8a` | 淺金（數字、icon） |
| `--text` / `--muted` | 米白／灰綠 | 內文／次要文字 |
| `--serif` | Noto Serif TC | 宋體標題 |
| `--sans` | 蘋方/Noto Sans TC | 內文 |
| `--title` | `clamp(46px,15vw,104px)` | 首屏「生菜王子」字級 |

**改顏色** = 直接改這些變數值，全站連動。

---

## 3. 常見修改怎麼做

| 要改什麼 | 去哪裡改 |
|---|---|
| **換首屏溫室照** | 換 `assets/greenhouse-hero.jpeg`；亮度在 CSS `.hero__bg img{ filter:brightness(...) }`；構圖角度在同行 `object-position:center`（往右給 60%、往下給 `center 70%`） |
| **首屏文字/標語** | HTML `HERO` 段：`.hero__brand`(品牌)、`<h1>`(生菜王子)、`.hero__tag`(標語) |
| **一句話定位 / 跳躍照** | `STATEMENT` 段 |
| **戰績四個數字** | `STATS` 段的四個 `.stat`；米其林花標數量＝`.michelin` 內 `<svg><use href="#florette"/></svg>` 的個數 |
| **事業版圖三張卡** | `VENTURES` 段 `.v-card`（注意：對外不寫「德馳」，見注意事項） |
| **創業時間軸 / 興趣** | `ABOUT` 段 `.timeline` 四個 `<li>`、`.traits` |
| **電話/Email/地址** | `CONTACT` 段 `.info`；**同步改 vCard**（見下） |
| **社群連結（LINE/FB/IG）** | `CONTACT` 段 `.social-rows`（上排3＋下排3），改 `href` |
| **vCard（存通訊錄）內容** | 檔案最底 `<script>` 內的 `BEGIN:VCARD…`（電話/email/地址要與畫面一致） |

---

## 4. 部署

```bash
cd ~/Projects/robert-intro-page
git add -A && git commit -m "說明" && git push origin main
# 約 30–60 秒後線上更新；驗證：curl -s -o /dev/null -w "%{http_code}" 線上網址
```

---

## 5. ⚠️ 注意事項（接手前必看）

1. **截圖假象**：headless Chrome 有最小視窗 500px，設 375/430 會被當 500 算，右邊會「看起來被切」。**這不是真 bug**。要看真實手機版面：用真手機，或截圖時 `--window-size=500,…`。
2. **德馳/DECHI 對外不公開**（業主怕商標被冒用）：公開頁只能寫「未來農場 / 國際貿易」，不可出現「德馳、DECHI、以德為本、馳向未來」。內部文件才用德馳。
3. **米其林花標**：是自繪 SVG（`#florette` symbol），非官方商標檔。商標風險已向業主說明、業主仍要用，這是業主決定。文案維持事實陳述「米其林餐廳指定」。
4. **口徑**：講「歐洲」不講「荷蘭」（與 BNI 口徑一致）。

---

## 6. 未來可能調整清單（Backlog）

- [ ] **自訂網域**：業主買 `robert-hortitech.com` 後，加 DNS CNAME 指到 `robbiechiu.github.io`＋repo 放 `CNAME` 檔＋Pages 設定填網域 → `card.robert-hortitech.com`。
- [ ] **NFC 卡**：把線上網址寫入 NFC 卡，碰一下傳名片（教業主用手機 App 寫卡）。
- [ ] **hero 角度微調**：業主可能要再調 `object-position`（多露生菜／多露溫室）。
- [ ] **戰績數字最終確認**：66倍/10倍/1000噸 是否公開無誤。
- [ ] **LINE 已是按鈕**；若 FB/IG 帳號有變更要更新 href。
- [ ] **可能新增**：頁面自身 QR（給人掃進名片）、favicon、og:image（分享預覽圖）、SEO meta。
- [ ] **另一頁**：賣生菜給餐廳/通路的版本（業主早期提過，與這張通用名片分開做）。
- [ ] 未使用的 `assets/qr-*.png` 與 `.qr-single` CSS 可清理（目前留著）。
